<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        xmlns:tools="http://schemas.android.com/tools"
        tools:context=".ui.character.CharacterActivity">

    <!-- Other components and views -->

    <com.google.android.material.bottomappbar.BottomAppBar
            android:id="@+id/bottom_bar_character"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            app:fabCradleMargin="5dp"
            app:backgroundTint="@color/colorPrimary"
            app:navigationIcon="@drawable/ic_menu"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_button_character"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_looks_6"
            app:backgroundTint="@color/colorAccent"
            app:layout_anchor="@id/bottom_bar_character"
            app:shapeAppearanceOverlay="@style/FloatingActionButton.Shape.Dice"/>

    <include layout="@layout/content_character" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>